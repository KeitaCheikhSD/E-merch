{% extends 'base.html.twig' %}

{% block body %}

<div class="flex items-center justify-center min-h-screen bg-gradient-to-r from-purple-500 via-pink-500 to-red-500 text-white">
  <h1 class="text-4xl md:text-6xl font-extrabold text-center shadow-lg">
    <span class="block text-gray-200">Register</span>
    <input type="text" id="name" name="name" required minlength="4" maxlength="8" size="10" </input>
    <button>
    <span class="text-yellow-300 text-7xl md:text-9xl animate-pulse">{{ number }}</span>
  </h1>
</div>
{% endblock %}
